global:
  scrape_interval: 5s
  evaluation_interval: 5s
  scrape_timeout: 3s

scrape_configs:
  - job_name: telemetry_app
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:5000
